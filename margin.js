function (margin){
var price = {
  startingValue : 200;
  increment : function (){prompt("what is your monthly price increment?", "ex: 10");
  calculate: 
    function(){ 
      for(i = 0; i < classSize.max; i++){
         var monthlyPayment = 
              if(priceArray == true){ //does an empty array return true?
                  priceArrayStartingValue * 1}
              else {priceArrayStartingValue * priceArrayIncrement};
           priceArray.push(monthlyPayment);
      },
        
  array: [];
var priceArrayStartingValue = 200;
var classSize = { min: 3, max: 8 };
var studentQtyArray= [2,3,4,5,6,7,8];
var studentQtyArrayLength = studentQtyArray.length;
var resultArray=[];
var priceArrayIncrement=function (){prompt("what is your monthly price increment?", "ex: 10");

function calculatePriceArray(){ 
  for(i = 0; i < classSize.max; i++){
       var monthlyPayment = 
       		if(priceArray == true){ //does an empty array return true?
      			priceArrayStartingValue * 1}
       		else {priceArrayStartingValue * priceArrayIncrement};
   		 priceArray.push(monthlyPayment);

  for(i = 0; i < studentQtyArrayLength; i++) 
	priceArray[i] *
 
}
