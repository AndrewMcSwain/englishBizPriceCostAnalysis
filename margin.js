function (margin){

var price = {
  startingValue : 200;
  array: [],
  priceincrement : function (){prompt("what is your monthly price increment?", "ex: 10");}
  calculate: = 
    function(){ 
      for(i = 0; i < classSize.max; i++){
         var monthlyPayment = [];
              if(price.array == true){ //does an empty array return true?
                  price.startingValue * 1}
              else {priceArrayStartingValue * priceArrayIncrement};
           priceArray.push(monthlyPayment);
              }
    	return monthlyPayment;
           },
       };

var classSize = { min: 3, max: 8 };

var studentQtyArray= [2,3,4,5,6,7,8];

var studentQtyArrayLength = studentQtyArray.length;

var priceList = price.calculate();

var resultArray = studentQtyCalc();
    
for(i = 0; i < studentQtyArrayLength; i++) 
	
 
}
